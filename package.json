{
  "name": "bible-study-wheel",
  "version": "0.5.0",
  "private": true,
  "description": "Amplify Gen 2 backend definition. Frontend is in frontend/, local-dev backend in backend-local/, Lambda handler source in backend-aws/functions/.",
  "scripts": {
    "sandbox": "ampx sandbox",
    "sandbox:delete": "ampx sandbox delete",
    "deploy": "ampx pipeline-deploy",
    "sync-config": "node scripts/sync-amplify-config.mjs"
  },
  "devDependencies": {
    "@aws-amplify/backend": "^1.0.0",
    "@aws-amplify/backend-cli": "^1.0.0",
    "@types/node": "^25.9.1",
    "aws-cdk-lib": "^2.150.0",
    "constructs": "^10.0.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.1053.0",
    "@aws-sdk/client-dynamodb": "^3.1053.0",
    "@aws-sdk/client-ses": "^3.1053.0",
    "@aws-sdk/lib-dynamodb": "^3.1053.0"
  }
}
